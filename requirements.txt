# The exact-pin lock for WebATEM's OWN builds (the Docker image, the desktop
# bundles, CI): pyproject.toml carries ranges so the app can be installed as
# a library into a project with its own pins; this file applies the exact
# versions those builds were verified with. Dependabot bumps the device
# libraries here and in pyproject.toml together.
Django==6.0.7
channels==4.3.2
uvicorn[standard]==0.52.0
pillow==12.3.0
whitenoise==6.12.0
zeroconf==0.147.2
ifaddr==0.2.0
pystray==0.19.5
pywebview==6.2.1
atemwire==0.15.0.dev1
hyperdeckwire==0.1.0.dev1
pytest==9.1.1
pytest-django==4.12.0
pytest-timeout==2.4.0
