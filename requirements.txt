Django==6.0.7
channels==4.3.2
uvicorn[standard]==0.52.0
pillow==12.3.0
whitenoise==6.12.0
zeroconf==0.147.2
# Blackmagic device libraries (PyPI; github.com/lucas-romanenko/<name>).
# atemwire ships an sdist and builds its mediaconvert C extension at
# install time — a C compiler must be present (the Dockerfile and the
# desktop build both have one).
atemwire==0.15.0.dev0
hyperdeckwire==0.1.0.dev0
pytest==9.1.1
pytest-django==4.12.0
pytest-timeout==2.4.0
