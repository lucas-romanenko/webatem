<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Hardened-runtime entitlements a PyInstaller-frozen Python needs:
       CPython and its extension modules JIT nothing, but ctypes and the
       loader map executable memory and load dylibs that are not signed by
       the same team (the bundled Python framework, pywebview's backend).
       Notarization requires the hardened runtime; these three keep it from
       killing the app at launch. -->
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
  <!-- The launcher listens on the LAN and talks to switchers. -->
  <key>com.apple.security.network.server</key>
  <true/>
  <key>com.apple.security.network.client</key>
  <true/>
</dict>
</plist>
